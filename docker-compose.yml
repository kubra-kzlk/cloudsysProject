version: "3.8"
services:
  php-apache:
    image: kubrakizilkilic/my-php-apache-app:latest
    ports:
      - "8080:80"
    volumes:
      - /var/www/html
    depends_on:
      - db
  db:
    image: kubrakizilkilic/mariadb:latest
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: backend
